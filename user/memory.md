# Memory

## Setup

**Follow this section before other tasks while it is present.** When finished, **delete this entire `## Setup` section** (from this heading through the last line below) using `memory_delete` or `memory_replace`. Do not leave these instructions in memory afterward.

1. In 1–2 natural turns, ask for: preferred name, age range (not exact birthdate), occupation/role, preferred tone (formal/casual).
2. Save what you learned with `memory_append` under a `## User` section (create it if needed).
3. Delete this whole `## Setup` section from `memory.md`.

Do not demand secrets or exact birthdates.
